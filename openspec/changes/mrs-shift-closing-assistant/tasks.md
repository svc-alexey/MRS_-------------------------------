# Tasks: Помощник закрытия смены (`mrs-shift-closing-assistant`)

Порядок фаз — `design.md → ## Build Sequence` (1:1 по номерам фаз ниже). Все архитектурные решения зафиксированы в `proposal.md`/`design.md`/6 delta-спеках и не пересматриваются здесь — каждая задача только раскладывает решение на конкретный файл/процедуру/объект.

**Жёсткое ограничение на весь change:** ни одна задача не должна редактировать `CommonModules/MRS_МенеджерОбменаTNG/Ext/Module.bsl` и `CommonModules/MRS_МенеджерОбменаMicros/Ext/Module.bsl` — только вызов их существующих экспортных функций (`ВыполнитьЗагрузкуДанныхTNG/Micros`, `ВыполнитьПолучениеДанныхTNG/Micros`) без единой правки внутри этих файлов (`design.md → Решение 4/9`).

---

## Фаза 1 — Общий модуль: каркас

- [ ] 1.1 Создать метаданных-объект общего модуля: `CommonModules/MRS_ПомощникЗакрытияСменыСервер.xml` (свойства: `Сервер=Истина`, `ВнешнееСоединение=Истина`, клиентские компиляции — `Ложь`, привилегированный режим — не включать, см. `design.md → Critical Details → Контекст выполнения`).
  Риск/откат: новый объект, UUID генерируется платформой заново — коллизий нет; откат — удалить объект до коммита.
  Проверка: `verify_xml`.
- [ ] 1.2 Создать `CommonModules/MRS_ПомощникЗакрытияСменыСервер/Ext/Module.bsl` с тремя пустыми регионами по `module-structure.mdc → Common Module`: `#Область ПрограммныйИнтерфейс`, `#Область СлужебныйПрограммныйИнтерфейс`, `#Область СлужебныеПроцедурыИФункции`.
  Проверка: `syntaxcheck` каркаса.

## Фаза 2 — Срез 1: «Сопоставление объектов фронта» (домен `smena-front-object-matching`)

- [ ] 2.1 В `#Область ПрограммныйИнтерфейс` реализовать `Функция СопоставитьОбъектФронта(ТипОбъекта, ФронтСистема, Код, Объект) Экспорт` — точно по коду из `design.md → Решение 6` (поиск набора записей `РегистрыСведений.ПЛ_СоответствиеСФронтСистемами` по отбору `ТипОбъекта`/`ФронтСистема`/`Код`, создание записи при отсутствии, заполнение `Объект`, `Записать()`). Покрывает `smena-front-object-matching → Requirement: Единая точка сопоставления...`, `Requirement: Новая форма сопоставления...` (программная часть).
  Проверка: `syntaxcheck`.
- [ ] 2.2 В `#Область ПрограммныйИнтерфейс` реализовать `Функция СтатусШагаСопоставление(День, МассивСкладов) Экспорт` — запрос по чекам `питДанныеПродажФронта` выбранного дня/фронт-систем, ЛЕВОЕ соединение с `ПЛ_СоответствиеСФронтСистемами` по коду объекта (аналог запроса из `ManagerModule.bsl → ОбработатьЧеки`, но `SELECT`-только, без побочных эффектов, `design.md → Решение 6`), отбор строк с незаполненным `Объект`. Возвращает и статус («Пройден»/«НеПройден»), и сами строки несопоставленных объектов для заполнения таблицы `НесопоставленныеОбъекты` на форме (форма записи возврата — структура с полями `Статус`/`Строки` или аналог, точное имя/состав — на усмотрение реализатора, консистентно с остальными `СтатусШагаX`). Покрывает `smena-front-object-matching → Requirement: Единая точка сопоставления...`, `Requirement: Блокировка формирования документов учёта по несопоставленным объектам`.
  Проверка: `syntaxcheck`; ручной вызов из консоли/обработчика на тестовой ИБ с известным набором несопоставленных кодов — сверить, что список строк совпадает с ожидаемым и статус «НеПройден» при наличии хотя бы одного несопоставленного объекта.

## Фаза 3 — Срез 2: «Сверка чеков» (домен `smena-sales-reconciliation` + `smena-front-data-load`)

- [ ] 3.1 В `#Область СлужебныйПрограммныйИнтерфейс` реализовать `Функция ПолучитьОтклоненияСверки(День, МассивСкладов) Экспорт` — вызывает существующие `ОбщийМодуль.MRS_МенеджерОбменаTNG.ВыполнитьПолучениеДанныхTNG(...)`/`ОбщийМодуль.MRS_МенеджерОбменаMicros.ВыполнитьПолучениеДанныхMicros(...)` (только вызов, без правок этих модулей) и строит построчные отклонения: пропускает строки с незаполненным `ЧекОбщепита` (не отклонение, `smena-front-data-load → Requirement: Незакрытые на фронте чеки...`), сравнивает суммы/скидки по остальным (`smena-sales-reconciliation → Requirement: Построчный вывод отклонений сверки`), помечает строки с `АлкогольнаяПродукция=Истина` и не найденной маркой через `ОбщийМодуль.MRS_ЗагрузкаЕГАИСФронт.ПолучитьАкцизнуюМаркуПоНомеруЧека(...)` отдельной категорией (`smena-sales-reconciliation → Requirement: Отклонения по алкоголю и маркам...`).
  Проверка: `syntaxcheck`.
- [ ] 3.2 В `#Область ПрограммныйИнтерфейс` реализовать `Функция СтатусШагаСверка(День, МассивСкладов) Экспорт` — статус «Пройден» только при пустом результате 3.1 (`smena-sales-reconciliation → Requirement: Любое ненулевое отклонение блокирует переход`); при пустом результате отдельно сигнализирует «всё чисто» (`Requirement: Явное «всё чисто»...`); если за день нет ни одного документа `питДанныеПродажФронта` — отдельный признак «данных нет» (`smena-front-data-load → Requirement: Данные дня считаются доступными для сверки`).
  Проверка: `syntaxcheck`; ручной вызов на тестовой ИБ на дне с известным расхождением в 1 копейку — статус должен быть «НеПройден».
- [ ] 3.3 В `#Область ПрограммныйИнтерфейс` реализовать `Процедура ПерезагрузитьЧекиЗаДень(День) Экспорт` — вызывает `ВыполнитьЗагрузкуДанныхTNG(...)`/`ВыполнитьЗагрузкуДанныхMicros(...)` за выбранный день по обеим фронт-системам (только вызов, без правок этих модулей). Используется точечным действием «(Пере)загрузить чеки за день» на шагах «Сопоставление» и «Сверка» (`smena-front-data-load → Requirement: Действие «(Пере)загрузить чеки за день» на шаге сверки`, `Requirement: То же действие доступно на шаге сопоставления...`). Без обёртки БСП внутри самой процедуры — обёртка `ДлительныеОперации.ВыполнитьПроцедуру` добавляется формой в Фазе 7.
  Проверка: `syntaxcheck`; ручной вызов на тестовой ИБ на дне без загруженных чеков — после вызова документы `питДанныеПродажФронта` должны появиться (`smena-front-data-load → Scenario: Данных за день нет вообще`).

## Фаза 4 — Срез 3: «Формирование целевых документов» (домен `smena-target-document-generation`, `design.md → Решение 4`)

- [ ] 4.1 В `#Область СлужебныйПрограммныйИнтерфейс` реализовать `Функция ЧекиЗагруженыПолностью(День, Склад) Экспорт` — сравнение множества номеров чеков фронта (через `ВыполнитьПолучениеДанныхTNG/Micros`, тот же источник, что в 3.1) с множеством номеров чеков, загруженных в `питДанныеПродажФронта` за день/склад; «полная загрузка» = пустая разница. Покрывает `smena-target-document-generation → Requirement: Предусловие полноты загрузки чеков за день`.
  Заметка (Open Question design.md №1): дефолт принят как единственный доступный критерий (нет отдельного флага «день закрыт на фронте»); уточнить на тестовой ИБ, не блокирует реализацию.
  Проверка: `syntaxcheck`; ручной вызов на дне с частичной загрузкой — должен вернуть `Ложь` (`Scenario: Частичная загрузка блокирует генерацию`).
- [ ] 4.2 В `#Область ПрограммныйИнтерфейс` реализовать `Функция ЕстьНеактуальныеВыпуски(День, Склад) Экспорт` — запрос к `РегистрСведений.питУчетДанныхПродаж` строго по коду из `design.md → Решение 4, Шаг 2` (`Дата = &ВыбранныйДень И КассовыйУзел.СкладПриготовления = &Склад И НЕ ОбновленПриемник`). Покрывает `Requirement: Проверка актуальности выпусков и предложение пересоздания при входе на шаг`.
  Проверка: `syntaxcheck`; ручной вызов на дне/складе с известной записью `ОбновленПриемник=Ложь` — должен вернуть `Истина`.
- [ ] 4.3 В `#Область СлужебныйПрограммныйИнтерфейс` реализовать `Процедура ПометитьЧекиКПересозданию(День, Склад) Экспорт` — эквивалент паттерна `УстановитьВозможностьЗагрузкиНаСервере` из `Forms/ВозможностьЗагрузкиДПФ` (не вызывается программно чужая форма — логика воспроизводится своя, `design.md → Discovered Patterns п.10`): запрос к `питУчетДанныхПродаж` с отбором `Дата=&День И КассовыйУзел.СкладПриготовления=&Склад И ОбновленПриемник ≠ Истина` (включая ветку чеков-возвратов `ВидОперации=...ДанныеПродажФронтаВозврат`), для каждого найденного `ДокументРегистратор` — `ПолучитьОбъект().Записать()` в `Попытка/Исключение`.
  Проверка: `syntaxcheck`.
- [ ] 4.4 В `#Область СлужебныйПрограммныйИнтерфейс` реализовать `Процедура ЗаполнитьПараметрыЗагрузкиОбработки(Обработка, День, Склад) Экспорт` — воспроизводит дефолты `Инициализация()` формы `питЗагрузкаИзДанныхПродажФронта/Forms/Форма` (`ДатаЦелевыхДокументов=1`, `ГруппироватьПоНомеруСмены=Истина`, `ПереходящаяСмена=Истина`, `ДнейВпередДляАнализаПерехода=1`, `ДнейНазадДляАнализаПерехода=1`), устанавливает `ДатаНачалаОтбора=ДатаКонцаОтбора=День`, заполняет ТЧ `ПараметрыКасс` только кассами, чей `питКассовыйУзел.СкладПриготовления=Склад` (запрос по `Справочник.КассыККМ`).
  Проверка: `syntaxcheck`.
- [ ] 4.5 В `#Область ПрограммныйИнтерфейс` реализовать `Процедура СформироватьЦелевыеДокументы(День, Склад, АдресРезультата) Экспорт` — оркестрация: 4.3 → 4.4 → `Обработки.питЗагрузкаИзДанныхПродажФронта.СоздатьОбъект().ВыполнитьЗагрузкуДанных()` (только вызов чёрного ящика, без правок) в `Попытка/Исключение` с `ЗаписьЖурналаРегистрации` при неожиданном исключении → результат (документ + `ПолучитьСообщенияПользователю(Истина)`) в `АдресРезультата`. Покрывает `Requirement: Действие «Переформировать выпуска»`.
  Заметка (Open Question design.md №2): права на запись `питДанныеПродажФронта`/вызов `ВыполнитьЗагрузкуДанных()` — рабочая гипотеза «базовых прав достаточно, привилегированный режим не нужен»; если на тестовой ИБ прав не хватит — добавить `УстановитьПривилегированныйРежим(Истина)` на время выполнения этой процедуры, без пересмотра архитектуры.
  Проверка: `syntaxcheck`; изолированный ручной вызов `MRS_ПомощникЗакрытияСменыСервер.СформироватьЦелевыеДокументы(День, Склад, АдресРезультата)` из консоли/обработчика на тестовой ИБ (`design.md → Critical Details → Тестируемость решения 4`) — без открытия формы мастера, без запуска регламентных заданий.
- [ ] 4.6 В `#Область СлужебныйПрограммныйИнтерфейс` реализовать `Функция ПолучитьОтклоненияВыпуска(День, Склад) Экспорт` — запрос с двумя временными таблицами и ПОЛНЫМ соединением по коду из `design.md → Решение 7` (агрегат по `питДанныеПродажФронта.Товары` vs агрегат по `питВыпускБлюд.Товары` где `Проведён=Истина`, характеристика игнорируется при `питПродажаПоСвободнойЦене`, без рецептурных коэффициентов). Разница `<0` — блокирующая строка, `>0` — информационная пометка. Покрывает `Requirement: Сверка чеков и выпусков`.
  Проверка: `syntaxcheck`; ручной вызов на известном расхождении (выпущено 7 из 10 ожидаемых по чекам) — должна вернуться строка дефицита 3 (`Scenario: Отрицательное отклонение блокирует шаг`).
- [ ] 4.7 В `#Область СлужебныйПрограммныйИнтерфейс` реализовать `Функция ПолучитьОшибкиФормирования(День, Склад) Экспорт` (имя на усмотрение реализатора) — классификация сообщений из 4.5 (`ПолучитьСообщенияПользователю`) по категориям «нехватка марок/акта», «нехватка ингредиентов», «отрицательные остатки после выпуска», «прочие» (по ключевым словам/структуре наблюдаемых сообщений — точный текст не подтверждён чтением кода, `design.md → Open Questions №3`, уточняется по факту на тестовых данных). Покрывает `Requirement: Явный список ошибок формирования`, `Requirement: Отсутствие рецептуры или производственного подразделения — не ошибка` (отсутствие рецептуры/подразделения не должно попадать в список ошибок).
  Проверка: `syntaxcheck`.
- [ ] 4.8 В `#Область СлужебныйПрограммныйИнтерфейс` реализовать `Функция ПолучитьДокументыОткрытыеЦены(День, Склад) Экспорт` — запрос `питВыпускБлюд` с заполненным `ПЛ_Номенклатура` за день/склад; критерий «требует внимания» — `Проведён ≠ Истина` (заполненность `ПЛ_Номенклатура` не используется как критерий, реквизит не очищается). Покрывает `Requirement: Отдельный список документов по открытым ценам`, `Requirement: Критерий пройденности шага по открытым ценам — факт проведения`.
  Проверка: `syntaxcheck`.
- [ ] 4.9 В `#Область ПрограммныйИнтерфейс` реализовать `Функция СтатусШагаФормирование(День, Склад) Экспорт` — объединяет 4.1 (предусловие), 4.2 (актуальность → признак «нужен диалог»), 4.6 (отрицательные отклонения блокируют), 4.7 (необработанные ошибки блокируют), 4.8 (непроведённые открытые цены блокируют). Статус «Пройден» только при выполнении всех пяти условий из `Requirement: Критерий пройденности шага «Формирование целевых документов»`; отрицательные остатки после выпуска — не блокируют этот шаг (проверяются шагом 5).
  Проверка: `syntaxcheck`; ручной вызов на дне, где все документы проведены, но есть отрицательный остаток — статус должен быть «Пройден» (`Scenario: Всё проведено, но есть отрицательный остаток`).

## Фаза 5 — Срез 4: «Анализ отрицательных остатков» (домен `smena-negative-stock-resolution`)

- [ ] 5.1 В `#Область СлужебныйПрограммныйИнтерфейс` реализовать `Функция ПолучитьОтрицательныеОстатки(День, Склад) Экспорт` — переиспользует ТОЛЬКО запрос к `РегистрНакопления.ТоварыНаСкладах.ОстаткиИОбороты(...)` из `DataProcessors/MRS_АРМ_Закрытия_смены/Ext/ObjectModule.bsl` (без слоя `АнализДанных`/`ПостроительОтчетаАнализаДанных` — он избыточен для табличного списка на форме мастера, `design.md → Discovered Patterns п.6`), отбор строк с остатком `< 0`. Покрывает `Requirement: Отображение отрицательных остатков`.
  Заметка (Open Question design.md №5 / proposal.md Open Question №1): шаг остаётся `report_only` — задача не создаёт никаких исполнительных действий (перемещение/списание/правка рецептуры) из мастера, только отображение.
  Проверка: `syntaxcheck`.
- [ ] 5.2 В `#Область ПрограммныйИнтерфейс` реализовать `Функция СтатусШагаОстатки(День, Склад) Экспорт` — статус «Пройден» только при пустом результате 5.1; при пустом результате — признак «всё чисто». Покрывает `Requirement: Критерий пройденности шага анализа остатков`.
  Проверка: `syntaxcheck`; ручной вызов после исправления остатков вне мастера — статус должен пересчитаться в «Пройден» без ручного сброса (`Scenario: Остатки исправлены вне мастера`).

## Фаза 6 — Обработка `MRS_ПомощникЗакрытияСмены` и форма: метаданные, навигация, статичный UI

- [ ] 6.1 Создать метаданных-объект `DataProcessors/MRS_ПомощникЗакрытияСмены.xml` (без реквизитов объекта — весь стейт в форме, `DefaultForm=Форма`, `design.md → Component Design`).
  Риск/откат: новый объект, UUID свежий — коллизий нет; откат — удалить объект.
  Проверка: `verify_xml`.
- [ ] 6.2 Создать `DataProcessors/MRS_ПомощникЗакрытияСмены/Ext/ObjectModule.bsl` — минимальный, без бизнес-логики (по `module-structure.mdc → Object/Manager Module`, пустые регионы под препроцессорным блоком).
  Проверка: `syntaxcheck`.
- [ ] 6.3 Создать `DataProcessors/MRS_ПомощникЗакрытияСмены/Forms/Форма.xml` + `Forms/Форма/Ext/Form.xml` — структура страниц: верхний уровень `ГруппаОсновная` (`СтраницаПараметров` → `СтраницаШагов`), внутри `СтраницаШагов` — `ГруппаШагов` (`ОтображениеСтраниц=Мастер`) с 4 дочерними страницами `СтраницаСопоставление`/`СтраницаСверка`/`СтраницаФормирование`/`СтраницаОстатки` (`design.md → Решение 1/2`).
  Проверка: `verify_xml`.
- [ ] 6.4 Добавить реквизиты формы: `ВыбранныйДень` (Дата, состав — Дата), `РекомендуемыйДень` (Дата), `ТекущийСклад` (`СправочникСсылка.Склады`), таблицы значений `СкладыВыбор` (`Склад`/`Отмечен`), `НесопоставленныеОбъекты`, `ОтклоненияСверки`, `ОшибкиФормирования`, `ДокументыОткрытыеЦены`, `ОтклоненияВыпуска`, `ОтрицательныеОстатки`, `СтатусыШагов` (`design.md → Implementation Map п.2`).
  Проверка: `verify_xml`.
- [ ] 6.5 Добавить элементы формы: таблицы под соответствующие реквизиты п. 6.4 на страницах шагов, индикатор статусов (`СтатусыШагов` в виде таблицы/списка сверху `СтраницаШагов`), `НадписьГотовность` на итоговой странице/области.
  Проверка: `verify_xml`.
- [ ] 6.6 Добавить команды формы (без реализации обработчиков — на этом шаге только объявление в командном интерфейсе формы): `НачатьРаботу`, `Далее`, `Назад`, `Сопоставить`, `ПерезагрузитьЧекиЗаДень`, `ПереформироватьВыпуска`, `ОткрытьДокументВыпуска`, `ВыбратьДругойДень` (`design.md → Implementation Map п.2`).
  Проверка: `verify_xml`.
- [ ] 6.7 В `DataProcessors/MRS_ПомощникЗакрытияСмены/Forms/Форма/Ext/Form/Module.bsl` создать 5 обязательных регионов по `module-structure.mdc → Form Module` (`ОбработчикиСобытийФормы`, `ОбработчикиСобытийЭлементовШапкиФормы`, регион(ы) `ОбработчикиСобытийЭлементовТаблицыФормы<ИмяТаблицы>` — по одному на каждую табличную часть из 6.4, `ОбработчикиКомандФормы`, `СлужебныеПроцедурыИФункции`).
  Проверка: `syntaxcheck` каркаса.
- [ ] 6.8 Реализовать обработчик `ПриОткрытии` (`&НаКлиенте`) — форма открывается на `СтраницаПараметров`; серверную процедуру заполнения `СкладыВыбор` из `Справочник.Склады` (все склады ИБ, без фильтрации по правам, `smena-shift-closing-workflow → Requirement: Выбор дня и складов на сессию мастера`).
  Проверка: `syntaxcheck`; ручное открытие формы на тестовой ИБ — список складов должен содержать все `Справочник.Склады`.
- [ ] 6.9 Реализовать обработчик команды `НачатьРаботу` (`&НаКлиенте`) — валидация «день + хотя бы один отмеченный склад заполнены» (иначе предупреждение и отказ от перехода, `Scenario: Открытие мастера без предварительного выбора`), сборка массива отмеченных складов в переменную формы, переход на `СтраницаШагов` (первая страница `ГруппаШагов`).
  Проверка: `syntaxcheck`.
- [ ] 6.10 Реализовать обработчик `ГруппаШагов.ПриАктивизации` (`&НаКлиенте` → тонкая `&НаСервере`-обёртка) — по текущей странице вызывает соответствующую `СтатусШагаX` серверную функцию (2.2/3.2/4.9/5.2, с текущим `ТекущийСклад` для складо-специфичных шагов 3/4) и обновляет строку `СтатусыШагов` и содержимое таблиц соответствующей страницы (`design.md → Решение 2`: пересчёт статуса на КАЖДУЮ активизацию, без кэширования дольше текущего открытия).
  Проверка: `syntaxcheck`; ручной проход по всем 4 страницам на тестовой ИБ — на каждой активизации видно свежий статус.
- [ ] 6.11 Реализовать обработчик команды `Далее` (`&НаКлиенте`) — запрашивает статус ТЕКУЩЕГО шага с сервера; если «Пройден» — переставляет `ГруппаШагов.ТекущаяСтраница` на следующую; иначе — предупреждение, остаётся на месте (`smena-shift-closing-workflow → Requirement: Жёсткая последовательность шагов, Scenario: Попытка перейти к сверке при незавершённом сопоставлении`).
  Проверка: `syntaxcheck`; ручной сценарий — с несопоставленным объектом переход на «Сверку» должен быть отклонён.
- [ ] 6.12 Реализовать обработчик команды `Назад` (`&НаКлиенте`) — переставляет `ТекущаяСтраница` на предыдущую БЕЗ проверки статуса (`Requirement: Жёсткая последовательность шагов, Scenario: Возврат к предыдущему шагу вручную`); статус пересчитывается заново самим `ПриАктивизации` при следующем открытии — никакого явного «откатывания» статуса в этом обработчике (`Requirement: Отсутствие автоматического отката статуса шага`).
  Проверка: `syntaxcheck`.
- [ ] 6.13 Реализовать обработчик команды `ВыбратьДругойДень` (`&НаКлиенте`) — сброс на `СтраницаПараметров` без сохранения прогресса (`design.md → Critical Details → Состояние формы не персистентно`).
  Проверка: `syntaxcheck`.
- [ ] 6.14 Реализовать вывод индикации готовности (`НадписьГотовность`) — серверная функция, агрегирующая статусы шагов 3/4 по КАЖДОМУ складу сессии (шаги 1/2 — по дню в целом, шаги 3/4 — по каждому складу, `proposal.md → Flows п.9`); текст явно поясняет, что это подсказка, а не факт продвижения даты (`smena-shift-closing-workflow → Requirement: Индикация готовности..., Scenario: Не все шаги пройдены по одному из складов` — явно указывает блокирующий склад). Мастер НЕ пишет в `РегистрСведений.ДатыЗапретаИзменения`.
  Проверка: `syntaxcheck`; ручной сценарий с двумя складами, один не пройден — надпись должна явно называть блокирующий склад.
- [ ] 6.15 В общий модуль `MRS_ПомощникЗакрытияСменыСервер`, `#Область ПрограммныйИнтерфейс`, реализовать `Функция РекомендуемыйДень(МассивСкладов) Экспорт` — пакетный запрос по диапазону дат (не цикл по дням), находит самый ранний день с хотя бы одним непройденным шагом по хотя бы одному из складов (`smena-shift-closing-workflow → Requirement: Подсветка рекомендуемого дня начала`). Горизонт диапазона — от максимальной `РегистрСведений.ДатыЗапретаИзменения.ДатаЗапрета` до текущей даты минус 1, разумный дефолт.
  Заметка (Open Question design.md №4): точная граница диапазона не зафиксирована `proposal.md` — принятый дефолт не блокирует реализацию, уточняется на реальных объёмах.
  Проверка: `syntaxcheck`; ручной вызов на тестовых данных с известным самым ранним непройденным днём (`Scenario: Есть непройденные дни в прошлом`) и на данных, где все дни закрыты (`Scenario: Все дни по всем складам закрыты` — явный ответ «непройденных дней не найдено», без резервного дня).

## Фаза 7 — Подключение БСП `ДлительныеОперации`/`ДлительныеОперацииКлиент` для 4 интерактивных операций (`design.md → Решение 5`)

- [ ] 7.1 Реализовать `&НаСервере`-обёртку + обработчик команды `ПерезагрузитьЧекиЗаДень` (`&НаКлиенте`) — запуск `ДлительныеОперации.ВыполнитьПроцедуру(ПараметрыВыполнения, "MRS_ПомощникЗакрытияСменыСервер.ПерезагрузитьЧекиЗаДень", ВыбранныйДень)`, `&НаКлиенте` — `ДлительныеОперацииКлиент.ОжидатьЗавершение` с текстом «Перезагрузка чеков за выбранный день...», по завершении — пересчёт статуса текущего шага (повторный вызов `ПриАктивизации`-логики).
  Проверка: `syntaxcheck`; ручной сценарий «(Пере)загрузить чеки за день» на шаге «Сверка чеков» на тестовой ИБ.
- [ ] 7.2 Реализовать `&НаСервере`-обёртку + обработчик команды `ПереформироватьВыпуска` (`&НаКлиенте`) — `ДлительныеОперации.ВыполнитьПроцедуру(ПараметрыВыполнения, "MRS_ПомощникЗакрытияСменыСервер.СформироватьЦелевыеДокументы", ВыбранныйДень, ТекущийСклад, АдресРезультата)`, `ОжидатьЗавершение`, по завершении — открытие документа выпуска через существующий шаблон печатной формы обработки `питЗагрузкаИзДанныхПродажФронта` (переиспользуется как есть) + показ ошибок проведения рядом (`smena-target-document-generation → Requirement: Действие «Переформировать выпуска», Scenario: Переформирование по явному действию пользователя`).
  Проверка: `syntaxcheck`; изолированный ручной сценарий на тестовой ИБ — запуск действия «Переформировать выпуска» и проверка, что документ показан пользователю с шаблоном печатной формы.
- [ ] 7.3 Реализовать обработчик активизации страницы «Формирование целевых документов» (`&НаКлиенте`) — при входе вызывает `ЕстьНеактуальныеВыпуски(День, ТекущийСклад)` (4.2); если `Истина` — `ПоказатьВопрос` с текстом «Рекомендуется пересоздание выпусков блюд для обновления. Выполнить сейчас?» (Да/Нет, асинхронный обработчик оповещения); «Нет» — шаг остаётся на паузе, без вызова 7.2; «Да» — программно вызывает ТОТ ЖЕ обработчик, что 7.2, без повторного открытия диалога (`Requirement: Проверка актуальности выпусков и предложение пересоздания при входе на шаг`).
  Проверка: `syntaxcheck`; ручные сценарии «Да»/«Нет» на тестовой ИБ (`Scenario: Обнаружена неактуальность — пользователь подтверждает пересоздание`, `Scenario: Пользователь отказывается — шаг остаётся на паузе`).
- [ ] 7.4 Реализовать `&НаСервере`-обёртку + обработчик команды `Сопоставить` (`&НаКлиенте`) — по каждой отмеченной строке `НесопоставленныеОбъекты` вызывает `СопоставитьОбъектФронта(...)` (2.1), затем СОБИРАЕТ множество уникальных значений `ФронтСистема` из ТОЛЬКО ЧТО сопоставленных строк и вызывает `ДлительныеОперации.ВыполнитьПроцедуру(ПараметрыВыполнения, "РегистрыСведений.ПЛ_СоответствиеСФронтСистемами.ЗаполнитьСсылкиПоКодамПослеЗагрузки", Новый Структура("ФронтСистема", ФронтСистема))` ОДИН РАЗ на каждое найденное значение (не готовая обёртка `...ВФоне` — жёсткий общий ключ, `design.md → Решение 6`, п. «Почему НЕ переиспользуется готовая обёртка»); `&НаКлиенте` — `ОжидатьЗавершение` с текстом «Обновление ссылок в загруженных чеках...», по завершении — удаление сопоставленных строк из таблицы `НесопоставленныеОбъекты` (`smena-front-object-matching → Requirement: Автоматическое дозаполнение ссылок...`).
  Проверка: `syntaxcheck`; ручной сценарий сопоставления кассы/номенклатуры на тестовой ИБ — после сопоставления ранее загруженные чеки с этим кодом должны получить заполненную ссылку без отдельной перезагрузки (`Scenario: Дозаполнение чеков сразу после сопоставления кассы`); отдельный ручной сценарий пакета из двух фронт-систем — оба вызова `ВыполнитьПроцедуру` не должны конфликтовать по ключу.

## Фаза 8 — Роль `MRS_УдалениеВыпускаБлюд`

- [ ] 8.1 Создать метаданных-объект `Roles/MRS_УдалениеВыпускаБлюд.xml` (Синоним — по смыслу роли) и права: право `Удаление=Истина` на заимствованный объект `Документ.питВыпускБлюд`, никакие другие права не выставляются (`design.md → Решение 8`). Покрывает `smena-target-document-generation → Requirement: Ограничение на удаление документа выпуска`.
  Риск/откат: новый объект расширения, UUID свежий — коллизий нет; откат — удалить роль (без зависимых объектов на момент создания). Роль работает как чистая аддитивная надстройка — не заменяет и не сужает базовую роль пользователя мастера.
  Проверка: `verify_xml`; ручные сценарии — пользователь без роли не может удалить `питВыпускБлюд` (`Scenario: Обычный пользователь не может удалить документ выпуска` — штатное поведение платформы, отдельного кода не требует), пользователь с ролью — может (`Scenario: Пользователь с повышенной ролью может удалить документ выпуска`).

## Фаза 9 — Скрытие `MRS_АРМ_Закрытия_смены` из командного интерфейса

- [ ] 9.1 Определить фактическую принадлежность `DataProcessors.MRS_АРМ_Закрытия_смены` подсистемам через `search_metadata`/`find_usages_of_object` (`1c-graph-metadata-mcp`) — найти, в составе каких подсистем (в т.ч. заимствованных подсистем основной конфигурации через `Ext/CommandInterface`) сейчас присутствует команда этого объекта.
  Проверка: результат поиска зафиксирован в отчёте задачи (список найденных подсистем либо явное «не найдено»).
- [ ] 9.2 Применить скрытие без правок кода `ObjectModule`/`Form`: дефолт — если найдена заимствованная подсистема (9.1), выставить `Visibility=Ложь` для команды `MRS_АРМ_Закрытия_смены` в её `Ext/CommandInterface.xml`; если подсистема не найдена в составе метаданных расширения — резервный вариант (на усмотрение реализатора) — скрытие через видимость команды на уровне ролей интерфейса (`Roles/MRS_ОсновнаяРоль.xml`) либо иной штатный механизм командного интерфейса, подтверждённый по факту на тестовой ИБ. `DataProcessors/MRS_АРМ_Закрытия_смены` физически остаётся в конфигурации (`proposal.md → Retired objects`) — правится только видимость. Покрывает `smena-shift-closing-workflow → Requirement: Замена текущей плоской обработки закрытия смены`.
  Риск/откат: правится только запись, относящаяся к `MRS_АРМ_Закрытия_смены`, в найденном файле командного интерфейса — остальные объекты того же файла не трогаются; откат — вернуть `Visibility=Истина`.
  Проверка: `verify_xml` для изменённого XML командного интерфейса/роли; ручная проверка на тестовой ИБ — команда `MRS_АРМ_Закрытия_смены` не отображается в интерфейсе обычного пользователя (`Scenario: Пользователь не обращается к старой обработке`).

## Фаза 10 — Сквозная верификация

- [ ] 10.1 `syntaxcheck` по каждому новому/изменённому BSL-модулю: `CommonModules/MRS_ПомощникЗакрытияСменыСервер/Ext/Module.bsl`, `DataProcessors/MRS_ПомощникЗакрытияСмены/Ext/ObjectModule.bsl`, `DataProcessors/MRS_ПомощникЗакрытияСмены/Forms/Форма/Ext/Form/Module.bsl` (повторный прогон на финальную версию файлов, после всех правок Фаз 1-7).
- [ ] 10.2 `check_1c_code` по тем же трём файлам.
- [ ] 10.3 `review_1c_code` по тем же трём файлам.
- [ ] 10.4 `verify_xml` по всем новым метаданных-объектам: `DataProcessors/MRS_ПомощникЗакрытияСмены.xml`, `Forms/Форма.xml`/`Ext/Form.xml`, `Roles/MRS_УдалениеВыпускаБлюд.xml`, изменённый XML командного интерфейса/роли из Фазы 9.
- [ ] 10.5 Ручной сценарий: изолированные вызовы `MRS_ПомощникЗакрытияСменыСервер.СформироватьЦелевыеДокументы(День, Склад, АдресРезультата)`, `ЕстьНеактуальныеВыпуски(День, Склад)`, `ЧекиЗагруженыПолностью(День, Склад)` из консоли/обработчика на тестовой ИБ — без открытия формы мастера, без запуска регламентных заданий (`design.md → Critical Details → Тестируемость решения 4`).
- [ ] 10.6 Подтвердить сверкой `git diff`/сравнением файлов, что `CommonModules/MRS_МенеджерОбменаTNG/Ext/Module.bsl` и `CommonModules/MRS_МенеджерОбменаMicros/Ext/Module.bsl` НЕ изменены ни одной строкой за весь change.
- [ ] 10.7 Чеклист соответствия каждому `### Requirement:` из 6 delta-спек реализации (закрывающий гейт этого change):

  **`smena-front-object-matching`**
  - [ ] Единая точка сопоставления касс и номенклатуры фронта — реализовано в 2.2 (список), 6.3/6.5 (UI одного экрана).
  - [ ] Новая форма сопоставления взамен типовой формы регистра — реализовано в 6.3, 2.1, 7.4 (без открытия `Forms/ФормаЗаписи`).
  - [ ] Автоматическое дозаполнение ссылок в загруженных чеках после сопоставления — реализовано в 7.4.
  - [ ] Блокировка формирования документов учёта по несопоставленным объектам — реализовано в 2.2, 6.11 (`Далее` не пускает дальше при статусе «НеПройден»).

  **`smena-shift-closing-workflow`**
  - [ ] Выбор дня и складов на сессию мастера — реализовано в 6.4, 6.8, 6.9.
  - [ ] Подсветка рекомендуемого дня начала — реализовано в 6.15, 7.3 (обёртка БСП, если применена к этому вызову).
  - [ ] Жёсткая последовательность шагов — реализовано в 6.11 (`Далее`), 6.10 (пересчёт статуса).
  - [ ] Отсутствие автоматического отката статуса шага — реализовано в 6.12 (`Назад` без проверки/сброса статуса).
  - [ ] Индикация готовности к продвижению даты запрета редактирования — реализовано в 6.14.
  - [ ] Замена текущей плоской обработки закрытия смены — реализовано в Фазах 2-7 (функциональность) + Фаза 9 (скрытие старой обработки).

  **`smena-sales-reconciliation`**
  - [ ] Построчный вывод отклонений сверки — реализовано в 3.1.
  - [ ] Явное «всё чисто» при отсутствии отклонений — реализовано в 3.2.
  - [ ] Любое ненулевое отклонение блокирует переход — реализовано в 3.2.
  - [ ] Отклонения по алкоголю и маркам видны как отдельная категория — реализовано в 3.1.

  **`smena-front-data-load`**
  - [ ] Действие «(Пере)загрузить чеки за день» на шаге сверки — реализовано в 3.3, 7.1.
  - [ ] То же действие доступно на шаге сопоставления объектов фронта — реализовано в 3.3, 7.1 (общая команда `ПерезагрузитьЧекиЗаДень` на обеих страницах).
  - [ ] Данные дня считаются доступными для сверки — реализовано в 3.2.
  - [ ] Незакрытые на фронте чеки не считаются отдельной категорией отклонения — реализовано в 3.1 (пропуск строк с незаполненным `ЧекОбщепита`).

  **`smena-negative-stock-resolution`**
  - [ ] Отображение отрицательных остатков — реализовано в 5.1, 5.2.
  - [ ] Критерий пройденности шага анализа остатков — реализовано в 5.2.

  **`smena-target-document-generation`**
  - [ ] Проверка актуальности выпусков и предложение пересоздания при входе на шаг — реализовано в 4.2, 7.3.
  - [ ] Предусловие полноты загрузки чеков за день — реализовано в 4.1, 4.9.
  - [ ] Действие «Переформировать выпуска» — реализовано в 4.3, 4.4, 4.5, 7.2.
  - [ ] Сверка чеков и выпусков — реализовано в 4.6.
  - [ ] Отсутствие рецептуры или производственного подразделения — не ошибка — реализовано в 4.7 (классификация не включает эти случаи).
  - [ ] Явный список ошибок формирования — реализовано в 4.7.
  - [ ] Отдельный список документов по открытым ценам — реализовано в 4.8.
  - [ ] Критерий пройденности шага по открытым ценам — факт проведения — реализовано в 4.8.
  - [ ] Критерий пройденности шага «Формирование целевых документов» — реализовано в 4.9.
  - [ ] Ограничение на удаление документа выпуска — реализовано в 8.1.
